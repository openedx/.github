#verifies setup.cfg doesn't have any non-ascii characters

name: ascii check

on:
  push:
    branches:
    - master
  pull_request:

jobs:
  ascii-check:
    uses: openedx/.github/.github/workflows/ascii-check.yml@master
